# Obsidian + Anki 사용 가이드

## 역할 분리

- Obsidian: 외국어 지식과 구조를 저장합니다.
- Anki: 반복해서 떠올려야 하는 내용을 복습하고 암기합니다.
- AnkiWeb: Anki를 데스크톱과 모바일에서 동기화합니다.

## 권장 흐름

1. 새로 배운 내용은 `00_Inbox/`에 먼저 적습니다.
2. 그다음 맞는 언어 폴더인 `01_Languages/` 아래로 옮깁니다.
3. 단어, 표현, 문법, 자주 쓰는 문장은 가능하면 항목 하나당 노트 하나로 작성합니다.
4. `03_Templates/`의 템플릿에 맞춰 frontmatter 필드를 넣습니다.
5. 장기 복습이 필요한 노트만 `anki: true`로 표시합니다.
6. 데스크톱 Obsidian에서 Anki로 내보낸 뒤, Anki Desktop을 AnkiWeb에 동기화합니다.
7. iPhone에서는 Anki를 AnkiWeb과 동기화해서 공부합니다.

## 폴더 규칙

- `01_Languages/`: 언어별 원본 노트 저장소입니다.
- `02_Review/`: 복습 대기 항목이나 카드 전환 후보를 둡니다.
- `03_Templates/`: 노트 템플릿을 둡니다.
- `04_Resources/`: 사전, 책, 참고 자료, 외부 자료를 둡니다.
- `05_Dashboard/`: 복습 대시보드와 구조 설명 문서를 둡니다.

## 노트 규칙

- 단어, 문법, 표현, 중요한 문장은 보통 노트 하나를 하나의 단위로 둡니다.
- 연습용 문장은 여러 개를 묶은 모음 노트로 두어도 됩니다.
- 모든 언어에 공통으로 쓰는 필드는 `language`, `type`, `level`, `source`, `status`, `anki`입니다.
- 언어별로 필요한 필드는 추가합니다. 예:
  - 중국어: `variety`, `script_variant`, `pinyin`, `tone`
  - 일본어: `kanji`, `kana`, `romaji`, `pitch_accent`
  - 아랍어: `arabic`, `transliteration`, `root`, `rtl`

## Anki 규칙

- 기본값은 `anki: false`입니다.
- 오래 기억해야 할 항목만 `anki: true`로 바꿉니다.
- 가능한 한 앞면/뒷면이 분명한 노트나 클로즈 형식을 사용합니다.
- 모든 노트를 Anki로 보내지는 않습니다.

## 매일 사용하는 법

- 배울 때는 먼저 적고, 나중에 정리합니다.
- 복습할 때는 `05_Dashboard/Study Dashboard.md`부터 봅니다.
- 새로 만든 노트의 기본 상태는 `draft`입니다.
- 처음 배우는 중이면 `learning`으로 둡니다.
- 복습하거나 실제로 자주 쓰는 항목은 `active`로 둡니다.
- 노트가 안정되면 `mastered`로 바꿉니다.
- 더 이상 적극적으로 보지 않으면 `archived`로 둡니다.

## capture 스킬 사용법

학습 내용을 직접 파일에 적지 않고 Codex에게 말하면, `capture` 스킬이 이 Vault의 적절한 위치에 노트를 만듭니다.

요청 예시:

```text
스페인어 단어 tener는 "가지다"라는 뜻이고 예문은 Tengo una pregunta.야. 노트로 추가해줘.
```

```text
일본어 문법 〜ている는 진행 중인 동작이나 결과 상태를 나타낸다는 내용을 추가해줘.
```

```text
중국어 간체 단어 学习, 병음 xuéxí, 뜻은 학습하다야. Anki 카드 후보로 추가해줘.
```

스킬은 다음 기준으로 처리합니다.

- 언어와 유형을 판단해서 `01_Languages/` 아래의 맞는 폴더에 노트를 만듭니다.
- 새 노트의 기본 상태는 `status: draft`입니다.
- 장기 복습 가치가 있으면 `anki: true`로 표시하고 `## Cards`에 Q/A 후보를 만듭니다.
- Anki로 보내는 최소 단위는 노트 전체가 아니라 `## Cards` 섹션의 Q/A입니다.
- 중국어에서 variety나 script가 불명확하면 먼저 질문합니다.
- 이미 비슷한 노트가 있으면 새 파일을 만들기보다 기존 노트 업데이트를 우선합니다.

## 첫 동기화 규칙

- 로컬 Anki 컬렉션이 비어 있고 AnkiWeb에도 데이터가 없으면 로컬 내용을 AnkiWeb으로 업로드합니다.
- AnkiWeb이 이미 기준 데이터라면 AnkiWeb에서 다운로드합니다.
- 첫 동기화가 끝나면 이후에는 일반 동기화만 사용합니다.

## 실무 추천

- Obsidian에는 모든 노트와 구조를 둡니다.
- Anki에는 복습할 가치가 있는 항목만 넣습니다.
- iPhone과 데스크톱을 맞추려면 AnkiWeb을 동기화 계층으로 사용합니다.
